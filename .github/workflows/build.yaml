name: Build iOS IPA

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Select Xcode Version
      run: sudo xcode-select -s /Applications/Xcode_15.4.app

    # НОВЫЙ ШАГ: Проверяем, что реально лежит внутри файла в облаке
    - name: Debug Print Package.swift
      run: |
        echo "=== СОДЕРЖИМОЕ PACKAGE.SWIFT ==="
        cat Package.swift
        echo "================================"

    - name: Hard Clean SPM Cache
      run: |
        rm -rf .build/
        rm -f Package.resolved
        rm -rf ~/Library/Caches/org.swift.swiftpm/

    # Передаем явный флаг отключения сетевого кэша самого гитхаба
    - name: Resolve Dependencies
      run: swift package --disable-sandbox resolve

    - name: Build iOS Application
      run: |
        xcodebuild build \
          -packagepath . \
          -scheme FanController \
          -destination "generic/platform=iOS" \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGN_IDENTITY="" \
          PROVISIONING_PROFILE_SPECIFIER="" \
          BUILD_DIR="build" \
          -configuration Release

    - name: Package App Binary into IPA
      run: |
        mkdir -p build/Payload
        find build -name "FanController.app" -type d -exec cp -r {} build/Payload/ \;
        cd build
        zip -r FanController.ipa Payload

    - name: Upload Finished IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: FanController-Unsigned-IPA
        path: build/FanController.ipa
        retention-days: 5
